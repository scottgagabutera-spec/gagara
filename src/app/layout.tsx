import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gagara · Programmable Escrow for Everyone",
  description: "Lock funds. Confirm conditions. Release with trust. Built on Interledger Open Payments.",
  openGraph: {
    title: "Gagara · Programmable Escrow for Everyone",
    description: "Lock funds. Confirm conditions. Release with trust.",
    siteName: "Gagara",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
